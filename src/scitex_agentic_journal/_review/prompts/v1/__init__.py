"""ARA reviewer prompts — v1.

These four markdown files are the canonical v1 prompts. They are
frozen — bumping a prompt body means bumping the version directory
to ``v2/``. A `ReviewRecord` stamped ``prompts_version="v1"`` always
refers to these exact files.
"""
